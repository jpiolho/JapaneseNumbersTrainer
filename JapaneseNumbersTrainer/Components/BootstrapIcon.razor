<i class="bi bi-@Icon @Class" />

@code {
    [Parameter] public string? Icon { get; set; }
    [Parameter] public string? Class { get; set; }

    private string? IconToCSSClass() => !string.IsNullOrEmpty(Icon) ? $"bi-{Icon}" : null;
}